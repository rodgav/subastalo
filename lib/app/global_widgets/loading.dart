import 'package:flutter/material.dart';

class LoadingWid extends StatelessWidget {
  const LoadingWid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
