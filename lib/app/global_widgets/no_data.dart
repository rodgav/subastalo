import 'package:flutter/material.dart';

class NoDataWid extends StatelessWidget {
  const NoDataWid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No data'),
    );
  }
}
